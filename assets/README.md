# Assets folder

Place `logo.png` here (a high-resolution, transparent PNG) when ready.
The Step 9 top-bar code will reference `assets/logo.png` directly --
no placeholder image is generated by design, per explicit instruction.
